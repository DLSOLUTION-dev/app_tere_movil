# URL base de la API del backend
# Desarrollo: usa tu IP local (ejecuta `ipconfig` en Windows / `ifconfig` en Mac)
# Producción: URL de tu servidor desplegado
EXPO_PUBLIC_API_URL=http://TU_IP_LOCAL:3000/api
